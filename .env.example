# Example .env - copy to .env and fill with real secrets (do NOT commit .env)
# Flask / app
FLASK_SECRET_KEY=change-this-secret-to-a-strong-random-value

# Postgres (used by both docker-compose and app)
POSTGRES_DB=hackyeah
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin1
POSTGRES_PORT=5432
DB_HOST=db
DB_PORT=5432
DB_NAME=hackyeah
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}
